[pytest]
asyncio_mode = auto
# One event loop for the whole run: the connection pool is a module-level
# singleton and psycopg refuses to reopen a closed one.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
